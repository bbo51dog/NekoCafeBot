services:
  voicevox:
    image: voicevox/voicevox_engine
    ports:
      - "50021:50021"
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      BOT_TOKEN: ${BOT_TOKEN}
    depends_on:
      - voicevox